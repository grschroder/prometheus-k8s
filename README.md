# Prometheus server on kubernetes

Deployment of a prometheus server on a kubernetes cluster using persistent volume.

**Change storageClassName on 'promserver-storage.yaml' to attach the fit your needs.**